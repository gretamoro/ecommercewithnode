<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>La mano invisible del mercado</title>
    <link href="https://fonts.googleapis.com/css?family=Dosis|Kaushan+Script" rel="stylesheet"/>
    <link rel="stylesheet" href="/stylesheets/style.css">
  </head>
  <body>
    <main class="main-container">
      <div class="menu">
        <nav>
          <h1><a href="http://localhost:3000/">La mano invisible del mercado</a></h1>
          <div class="session">
            <button type="button" name="button" id="logIn">Log in</button>
            <div id="userData" class="user hide">
              <h3 id="close">X Cerrar</h3>
              <input id="user" type="text" name="" value="" placeholder="nombre de usuaria"/>
              <input id="pass" type="password" name="" value="" placeholder="contraseña" />
              <button type="button" name="button" id="sendUserData"><a id="cargar" href="#">Iniciar Sesión</a></button>
            </div>
          </div>
          <div class="cart">
            <h2>Carrito</h2>
            <div id="empty">
              <p>Tu carrito está vacío.</p>
            </div>
            <ul id="productsCart">

            </ul>
            <h3>Total (¥): <span id="total">0</span></h3>

          </div>
        </nav>
      </div>
      <div class="products">
        {{#each photos}}
        <div class="cadaUno">
          <div class="img">
            <div class="divImg">
              <img src="{{this.url}}" alt="{{this.producto}}"/>
            </div>
          </div>
          <div class="info">
            <h2>{{this.producto}}</h2>
            <span class="yen">¥</span><span class="number">{{this.precio}}</span>
            <button class="add" type="button" name="button">Agregar al carrito</button>
          </div>
        </div>
        {{/each}}
      </div>
    </main>
    <script src="https://code.jquery.com/jquery-3.3.1.min.js"
      crossorigin="anonymous"></script>
    <script type="text/javascript" src="/javascripts/main.js"></script>
  </body>
</html>
